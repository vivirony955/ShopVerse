{
  "name": "@shopverse/plugin-us",
  "version": "0.1.0-alpha.1",
  "private": true,
  "description": "ShopVerse US region pack — registers a destination-based (per-state) sales-tax TaxStrategy. Disabled by default; enable in backend/plugins.config.ts for US deployments. Production: swap the starter rate table for a Stripe Tax / TaxJar adapter.",
  "license": "BUSL-1.1",
  "author": "Vivek Negi <vivironycrazy@gmail.com>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "@shopverse/sdk": "^0.1.0-alpha.1"
  },
  "devDependencies": {
    "@shopverse/sdk": "file:../../../packages/sdk",
    "@types/node": "^22.10.7",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
