# ─── DO NOT COMMIT WITH REAL VALUES ─────────────────────────────────────────
# This file is a template. Either:
#   (a) Use SealedSecrets / ExternalSecrets / Vault Agent and DELETE this file
#   (b) Substitute values and apply imperatively, NEVER committing the result
#   (c) Use `kubectl create secret generic` (see k8s/README.md for command)
#
# All values below are placeholders. Replace `__REPLACE_ME__` before apply.
# Reference k8s/README.md for the imperative create command (recommended for
# initial bootstrap).

apiVersion: v1
kind: Secret
metadata:
  name: shopverse-secrets
  namespace: ${NAMESPACE}
  labels:
    app.kubernetes.io/name: shopverse
    app.kubernetes.io/instance: shopverse
type: Opaque
stringData:
  # ─── Required ─────────────────────────────────────────────────────────────
  DATABASE_URL: "__REPLACE_ME__"
  JWT_SECRET: "__REPLACE_ME__"
  JWT_REFRESH_SECRET: "__REPLACE_ME__"
  STRIPE_SECRET_KEY: "__REPLACE_ME__"
  STRIPE_WEBHOOK_SECRET: "__REPLACE_ME__"
  SMTP_HOST: "__REPLACE_ME__"
  SMTP_PORT: "465"
  SMTP_USER: "__REPLACE_ME__"
  SMTP_PASS: "__REPLACE_ME__"
  SMTP_FROM: "__REPLACE_ME__"
  REDIS_URL: "__REPLACE_ME__"

  # ─── Optional (observability + admin tooling) ────────────────────────────
  # SENTRY_DSN: ""
  # METRICS_BASIC_AUTH: ""        # user:pass for /api/metrics
  # SWAGGER_USER: ""              # if you want /api/docs gated in prod
  # SWAGGER_PASS: ""
